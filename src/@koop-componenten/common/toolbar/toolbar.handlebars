<ul class="toolbar">
  {{#each items }}
  <li class="toolbar__item">
    <a {{#if id}}id="{{id}}"{{/if}} class="toolbar__link toolbar__link--{{ type }}" href="{{ link }}">{{#if counter }}{{ counter }}{{/if}} <span class="toolbar__label">{{ label }}</span></a>
  </li>
  {{/each}}
</ul>
