<div class="form__element">
  <label for="input--{{ id }}">{{ fieldLabel }}</label>
  {{#if fieldInstruction }}
    <small>{{ fieldInstruction }}</small>
  {{/if }}
  <input type="date" data-decorator="init-datepicker" id="input--{{ id }}" placeholder="{{ fieldPlaceholder }}" />
</div>

<div class="form__element">
  <label for="input--2">{{ fieldLabel }}</label>
  {{#if fieldInstruction }}
  <small>{{ fieldInstruction }}</small>
  {{/if }}
  <input type="date" data-decorator="init-datepicker" id="input--2" placeholder="{{ fieldPlaceholder }}" />
</div>
