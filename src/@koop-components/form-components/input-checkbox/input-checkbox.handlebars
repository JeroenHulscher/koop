<fieldset class="form__element checkboxgroup {{ modifier }}">
  <legend>{{ fieldLabel }} </legend>
  {{#each options }}
  <label for="option-{{ id  }}">
    <input type="checkbox" id="option-{{ id }}" name="{{ ../name }}" />{{ text }}
  </label>
  {{/each}}
</fieldset>
