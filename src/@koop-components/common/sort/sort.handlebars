<div class="sort {{ modifier }}">
  Sorteren op:
  <ul class="sort__options">
    {{#each options}}
      <li>
        <a href="?sortby={{value}}" class="sort--ascending">{{label}}</a>
      </li>
    {{/each}}
  </ul>
</div>
