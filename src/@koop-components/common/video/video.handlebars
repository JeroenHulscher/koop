<div class="video">
	<iframe
		title="{{ videoTitle }}" 
		width="560" 
		height="315" 
		src="{{ videoEmbedURL }}" 
		frameborder="0" allow="encrypted-media" allowfullscreen>
	</iframe>
	<a href="{{ videoURL }}">{{ videoTitle }} bekijken op Youtube</a>
</div>