<div class="topbar">
  <div class="container">
    {{#if navItems}}
    <nav class="topbar__nav">
      <ul>
        {{#each navItems}}
        <li>
          <a href="{{href}}" {{#if active}}class="is-active"{{/if}}>{{label}}</a>
        </li>
        {{/each}}
      </ul>
    </nav>
    {{/if}}
  </div>
</div>
