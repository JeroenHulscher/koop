{{#each items }}
	<div class="facet {{ checked }}">
		<label for="{{ id }}" >
			{{#if checked}}
				<input type="radio" id="{{ id }}" name="{{ group }}" checked />
			{{else}}
				<input type="radio" id="{{ id }}" name="{{ group }}" />
			{{/if}}
			{{ value }}
		</label>
		<span>{{ count }}</span>
	</div>
{{/each}} 