{{ render "@header" header merge=true }}
<div class="row row--page-opener">
  <div class="container">
    {{ render "@breadcrumb" }}
  </div>
</div>

<div class="container" id="content" role="main">
  <div class="row row--spacer">

    {{#> @jumbotron jumbotronContext }}
      <form class="form" data-decorator="init-form-validation">
        <fieldset class="u-nomargin">
          <legend class="visually-hidden">Doorzoek de decentrale overheden</legend>
          <div class="form__row form__row--medium">
            <div class="column form__row column-d-9">
              {{ render "@input-text--medium" inputZoekwoord merge=true }}
            </div>
          </div>

          <div class="columns">
            <div class="u-order--d2 align-right form__row">
              <button type="submit" class="form__button button button--primary button--large">Zoek regelingen</button>
            </div>
            <div class="u-order--d1 u-nomargin">
              <a href="#" class="cta">Uitgebreid zoeken</a><br>
            </div>
          </div>
        </fieldset>
      </form>
    {{/@jumbotron}}

  </div>

  <div class="row row--large">
    <div class="columns columns--dominant">
      <div class="column u-order--d2">{{render "@picture" }}</div>
      <div class="column u-order--d1 column--padded-med">
        {{render "@block"}}
      </div>
    </div>
  </div>

  <div class="row row--large">
    <div class="columns columns--dominant">
      <div class="column">{{render "@picture" }}</div>
      <div class="column column--padded-med">
         {{render "@block"}}
      </div>
    </div>
  </div>

  <div class="row">
    <div class="columns">
      {{ render "@block" block1Context }}
      {{ render "@block" block2Context }}
    </div>
  </div>

</div>

{{ render "@footer" }}
