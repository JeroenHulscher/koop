<div class="form__element">
  <label for="input--{{ id }}">{{ fieldLabel }}</label>
  <small>{{ fieldInstruction }}</small>
  <input type="date" class="input input--text" id="input--{{ id }}" placeholder="{{ fieldPlaceholder }}" />
</div>
